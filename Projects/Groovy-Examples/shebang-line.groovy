#!/usr/bin/env groovy
println "This is  from the unix shebang line"

/*allows the scripts to be run directly from the command-line, provided groovy is installed and the groovy command is available on the PATH.
Like : >shaebang-line.groovy
*/
